{% extends 'base.html.twig' %}

{% block title %}{{'produit.modifiertitre'|trans}}{% endblock %}

{% block body %}
    <h1>{{'produit.modifiertitre'|trans}}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('produit_index') }}">{{'produit.retour'|trans}}</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
